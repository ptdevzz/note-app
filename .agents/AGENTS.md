# Project Rules & Customizations for UsWeekends

## Command Execution & Approval
- Auto-execute terminal commands (`yarn build`, `git add`, `git commit`, `git push`, etc.) directly without prompting for confirmation.
- Maintain strict code syntax verification using `yarn build` before committing.
- Preserve custom Vietnamese dual-passcode architecture, PWA push notifications, and Firebase Realtime listeners.
